# Use a flat node_modules layout so Next.js's dev server can resolve its
# transitive peer deps (@swc/helpers, @next/env, etc.) the way it expects.
# pnpm's default strict isolation breaks `next dev` on this version (14.2.3).
node-linker=hoisted
